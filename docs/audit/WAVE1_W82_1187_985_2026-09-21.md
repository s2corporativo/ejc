# Wave 1 — W8.2, #1187 e #985

## #985 — governança do RAG

O item está corrigido na `main`. A implementação atual contém regressões para o caminho fail-closed de vigência, aprovação explícita de conteúdo jurídico e autoridade não normativa para proposições legislativas. As evidências estão nos testes `test_rag_normativo_fail_closed.py`, `test_knowledge_autoapproval.py`, `test_proposicao_legislativa_governanca.py` e nos commits de correção presentes na `main`.

## #1187 — migration 145

A migration histórica 145 ainda contém a guarda com `assert`, conforme a issue. Não foi editada nesta rodada porque a issue determina que uma migration já aplicada em produção não deve ser alterada retroativamente. A aplicação/provenance da migration no banco de produção não está disponível nesta sessão. O item permanece bloqueado até confirmar o histórico do banco; somente então deve ser criada uma correção compatível, preferencialmente uma nova migration/preflight com `RuntimeError` e teste de regressão.

## W8.2 — timeout de proxy de IA

O `ejc.conf` do Nginx é configuração do host e não está versionado neste repositório. Foi criado o runbook `docs/operacao/W8_2_IA_PROXY_TIMEOUT.md` com backup, `nginx -t`, reload, health check e curl sintético cronometrado. O item não é marcado como executado porque nenhuma alteração ou reload de produção foi realizado.
