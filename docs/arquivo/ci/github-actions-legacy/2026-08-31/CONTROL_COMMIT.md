# Commit de controle

Arquivo criado após a retirada completa de `.github/workflows/` para verificar se o workflow sintético `BuildFailed` ainda é registrado em pushes quando o repositório não possui GitHub Actions ativos.

Este arquivo não executa automação e não altera código, infraestrutura, banco de dados ou produção.
