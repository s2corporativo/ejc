# Provider Registry e avaliação comparativa

Entrega reaplicada diretamente sobre a `main` após o PR #322.

- registro único de providers;
- runtime conectado sem remover o fail-closed;
- comparação aditiva em `app.eval.compare_providers`;
- fallbacks fora das métricas do provider solicitado;
- FTS mantido opt-in até baseline jurídico real.

Não integrar sem backend, eval smoke e trajetória do agente verdes.
