name: Architecture Inventory — Phase 0

on:
  pull_request:

permissions:
  contents: read

concurrency:
  group: architecture-inventory-${{ github.ref }}
  cancel-in-progress: true

jobs:
  obsolete-branch:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "PR 468 substituída pela PR 469"
