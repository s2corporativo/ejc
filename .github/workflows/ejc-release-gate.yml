name: EJC Release Gate

on:
  pull_request:

permissions:
  contents: read

concurrency:
  group: ejc-release-gate-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  queue-drain:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "temporary queue drain"
