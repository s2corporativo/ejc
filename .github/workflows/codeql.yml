# APOSENTADO — GitHub Actions não é utilizado neste projeto.
#
# A análise de segurança foi migrada para o Woodpecker CI self-hosted em
# `.woodpecker.yml`, usando Semgrep Community Edition, Trivy e Gitleaks.
# Este arquivo é mantido temporariamente apenas para tornar a migração
# auditável e evitar reativação acidental do workflow antigo.
#
# Não há `on:` nem `jobs:`; portanto este arquivo não dispara GitHub Actions.
