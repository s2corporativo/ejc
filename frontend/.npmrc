# playwright (devDep p/ tests/responsividade.mjs) NÃO deve baixar navegador no
# install: o teste dirige um Chromium pré-instalado via PW_CHROMIUM/caminho do
# ambiente. Mantém `npm ci` do CI rápido e sem dependência de rede de browsers.
playwright_skip_browser_download=1
