# ── Segredos / ambiente ──────────────────────────────────────────────
.env
*.env
**/.env
!.env.example
!**/.env.example
vps-tools/.env
.env.bak*
.env.save
**/.env.bak*
**/.env.save

# Chaves e credenciais
*.pem
*.key
*.p12
*.pfx
*-service-account*.json
credentials*.json
config/gdrive_sa.json
config/rclone.conf

# ── Dependências / builds ────────────────────────────────────────────
node_modules/
**/node_modules/
backend/.venv/
backend/.venv-codex/
**/.venv*/
__pycache__/
**/__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
frontend/dist/
dist/

# ── Artefatos / backups / resíduos (não versionar) ───────────────────
*.tgz
*.tar.gz
*.zip
*.dump
*.db
*.sqlite
*.sqlite3
*.sql
*.sql.gz
*.log
uploads/
backups/
frontend/tests/__out__/
_QUARENTENA/
_dead_code/
_deploy_e2e1/
vps-tools/generated/
_session_componentes_bronze/
reports/*.csv

# ── Backups locais de arquivos ───────────────────────────────────────
*.bak
*.bak.*
Dashboard_light_backup.tsx
*.old
*.orig
check_*.py
chk*.py
tmp_*.py
test_s.py
seed_sumulas.py
verify_sumulas.py

# temporários de deploy (auditoria 01/07)
vps-tools/_*

# graphify — grafo de conhecimento local (regenerável, não versionar)
graphify-out/

# Worktrees efêmeros de subagentes (Claude Code)
.claude/worktrees/
.claude/launch.json
# Permissões locais do agente (por máquina/sessão) — nunca versionar
.claude/settings.local.json
frontend/.claude/

# Segredos aplicados direto na VPS (OAuth Google Drive etc.) — nunca versionar
secrets/

# Artefatos temporários do vps-tools (snapshots/testes manuais de SFTP)
vps-tools/_*

# Arquivos de rollback da reclassificação de área (contêm IDs de casos do
# escritório) — ver RUNBOOK_RECLASSIFICACAO_AREAS.md. Guardar fora do repo.
var/
