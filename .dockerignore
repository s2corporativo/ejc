.git
.env
**/.env
**/__pycache__/
**/*.pyc
**/.pytest_cache/
**/.mypy_cache/
**/.ruff_cache/
backend/.venv*
frontend/node_modules/
frontend/dist/
frontend/*.png
frontend/preview-design.html
vps-tools/
*.log
*.zip
*.tgz
*.tar.gz
*.bak
*.old
*.orig
uploads/
backups/
# Credenciais OAuth em runtime. Hoje é redundante (os builds usam
# `context: ./backend` e `./frontend`, e o Dockerfile faz COPY de
# subdiretórios explícitos, nunca `COPY . .`), mas fecha a simetria com
# `backups/` acima: no dia em que alguém adicionar um build com `context: .`,
# a proteção já está aqui em vez de virar incidente.
secrets/
_QUARENTENA/
_session_componentes_bronze/
ejc_frontend_current.tgz
